import{ChangeDetectionStrategy,Component,input,output}from'@angular/core';import{MatIconModule}from'@angular/material/icon';@Component({selector:'app-profile-access-card',standalone:true,imports:[MatIconModule],template:`<button type="button" (click)="selected.emit()"><mat-icon>{{icon()}}</mat-icon><span><strong>{{title()}}</strong><small>{{description()}}</small></span></button>`,styles:[`button{width:100%;display:flex;align-items:center;gap:10px;text-align:left;border:1px solid var(--border);background:white;border-radius:10px;padding:12px;cursor:pointer}strong,small{display:block}small{color:var(--muted);font-size:.7rem;margin-top:2px}`],changeDetection:ChangeDetectionStrategy.OnPush})export class ProfileAccessCardComponent{readonly title=input.required<string>();readonly description=input('');readonly icon=input('person');readonly selected=output<void>()}
