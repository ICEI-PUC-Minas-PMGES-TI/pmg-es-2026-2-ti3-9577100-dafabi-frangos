import{ChangeDetectionStrategy,Component,input}from'@angular/core';@Component({selector:'app-stock-level',standalone:true,template:`<div class="stock-level"><strong>{{quantity()}}</strong><span>{{unit()}}</span></div>`,styles:[`.stock-level{display:flex;align-items:baseline;gap:4px}.stock-level strong{font-size:1.05rem}.stock-level span{color:var(--muted);font-size:.72rem}`],changeDetection:ChangeDetectionStrategy.OnPush})export class StockLevelComponent{readonly quantity=input.required<number>();readonly unit=input.required<string>()}
