import { DatePipe } from '@angular/common';
import { ChangeDetectionStrategy, Component, computed, inject, signal } from '@angular/core';
import { MatButtonModule } from '@angular/material/button';
import { MatIconModule } from '@angular/material/icon';
import { MatSnackBar } from '@angular/material/snack-bar';
import { PageHeaderComponent } from '../../../../shared/components/page-header/page-header.component';
import { StatusChipComponent } from '../../../../shared/components/status-chip/status-chip.component';
import { SyncSummaryComponent } from '../../components/sync-summary/sync-summary.component';
import { SyncSummary } from '../../models/integration.models';
import { IntegrationsService } from '../../services/integrations.service';

@Component({selector:'app-ifood',standalone:true,imports:[DatePipe,MatButtonModule,MatIconModule,PageHeaderComponent,StatusChipComponent,SyncSummaryComponent],template:`<app-page-header eyebrow="Integrações" title="iFood" subtitle="Sincronize as vendas concluídas e canceladas do período."/><div class="integration-status surface"><span class="logo">iF</span><div><strong>Conexão com o iFood</strong><small>Última sincronização: {{integration().lastSync|date:'dd/MM/yyyy HH:mm'}}</small></div><app-status-chip [status]="integration().status"/></div><div class="sync-layout"><section class="surface surface-body"><h2>Sincronizar vendas</h2><p>Escolha o período. Vendas já importadas serão identificadas e ignoradas.</p><label>Período</label><div class="period-fields"><input class="filter-select" type="date" [value]="start()" (change)="start.set($any($event.target).value)"><span>até</span><input class="filter-select" type="date" [value]="end()" (change)="end.set($any($event.target).value)"></div>@if(!validPeriod()){<p class="field-error">Informe um período válido.</p>}<button mat-flat-button class="primary-button" (click)="sync()" [disabled]="loading()||!validPeriod()"><mat-icon>sync</mat-icon>{{loading()?'Sincronizando…':'Sincronizar vendas'}}</button>@if(loading()){<div class="progress"><span></span></div>}</section><aside class="surface surface-body"><h2>Tratamento automático</h2><ul><li><mat-icon>check</mat-icon>Impede vendas duplicadas</li><li><mat-icon>check</mat-icon>Atualiza histórico e dashboard</li><li><mat-icon>check</mat-icon>Baixa os itens do estoque</li><li><mat-icon>check</mat-icon>Recompõe estoque em cancelamentos</li></ul></aside></div>@if(summary()){<section class="surface surface-body result-block"><div class="notice success"><mat-icon>check_circle</mat-icon>Sincronização concluída com sucesso.</div><h2>Resumo da sincronização</h2><app-sync-summary [summary]="summary()!"/></section>}`,styles:[`.integration-status{display:flex;align-items:center;gap:13px;padding:14px 18px;margin-bottom:18px}.integration-status>div{flex:1}.integration-status strong,.integration-status small{display:block}.integration-status small{color:var(--muted);font-size:.72rem;margin-top:3px}.logo{width:42px;height:42px;border-radius:10px;background:#ea1d2c;color:#fff;display:grid;place-items:center;font-weight:800}.sync-layout{display:grid;grid-template-columns:1.15fr .85fr;gap:18px}.surface h2{font-size:1rem;margin:0 0 8px}.surface p{color:var(--muted);font-size:.8rem}.surface label{display:block;font-size:.72rem;font-weight:700;margin:20px 0 6px}.period-fields{display:flex;gap:8px;align-items:center;margin-bottom:12px}.sync-layout button{height:46px}.sync-layout ul{list-style:none;padding:0;margin:18px 0}.sync-layout li{display:flex;gap:8px;align-items:center;padding:8px 0;font-size:.78rem}.sync-layout li mat-icon{color:var(--success);font-size:18px;width:18px;height:18px}.field-error{color:var(--red-dark)!important}.progress{height:4px;background:#f2f0ef;overflow:hidden;border-radius:4px;margin-top:14px}.progress span{display:block;width:45%;height:100%;background:var(--red);animation:load 1s infinite}@keyframes load{from{transform:translateX(-100%)}to{transform:translateX(230%)}}.result-block{margin-top:18px;max-width:800px}@media(max-width:800px){.sync-layout{grid-template-columns:1fr}}`],changeDetection:ChangeDetectionStrategy.OnPush})
export class IfoodComponent{private readonly service=inject(IntegrationsService);private readonly snack=inject(MatSnackBar);readonly integration=computed(()=>this.service.find('IFOOD'));readonly loading=signal(false);readonly summary=signal<SyncSummary|null>(null);readonly start=signal('2026-09-15');readonly end=signal('2026-09-15');readonly validPeriod=computed(()=>Boolean(this.start()&&this.end()&&this.start()<=this.end()));async sync(){if(!this.validPeriod())return;this.loading.set(true);try{this.summary.set(await this.service.sync('IFOOD'));this.snack.open('Vendas do iFood sincronizadas.','Fechar',{duration:3500});}catch{this.snack.open('Integração indisponível. Tente novamente.','Fechar',{duration:4000});}finally{this.loading.set(false);}}}
