import{ChangeDetectionStrategy,Component,input,output}from'@angular/core';import{CurrencyPipe}from'@angular/common';import{MatIconModule}from'@angular/material/icon';import{Product}from'../../../../core/models/domain.models';
@Component({selector:'app-product-grid',standalone:true,imports:[CurrencyPipe,MatIconModule],template:`<div class="products-head"><div><h2>{{title()}}</h2><p>{{products().length}} opções disponíveis</p></div><span class="hint"><mat-icon>touch_app</mat-icon>Toque para adicionar</span></div><div class="product-grid">@for(product of products();track product.id){<button type="button" (click)="selected.emit(product)" [disabled]="product.stock===0"><span class="tile-icon">{{product.name.slice(0,2).toUpperCase()}}</span><span class="tile-copy"><strong>{{product.name}}</strong><small>{{product.category}} · {{product.stock}} em estoque</small></span><b>{{product.price|currency:'BRL':'symbol':'1.2-2':'pt-BR'}}</b>@if(product.stock===0){<span class="sold-out">Sem estoque</span>}</button>}</div>`,styles:[`.products-head{display:flex;justify-content:space-between;align-items:center;margin:8px 0 14px}.products-head h2{font-size:1rem;margin:0}.products-head p{color:var(--muted);font-size:.76rem;margin:4px 0 0}.hint{display:flex;align-items:center;gap:4px;color:var(--muted);font-size:.72rem}.hint mat-icon{font-size:17px;width:17px;height:17px}.product-grid{display:grid;grid-template-columns:repeat(3,minmax(145px,1fr));gap:10px}.product-grid button{position:relative;text-align:left;border:1px solid var(--border);background:white;border-radius:12px;padding:14px;min-height:132px;cursor:pointer;display:flex;flex-direction:column;align-items:flex-start;transition:.15s}.product-grid button:hover:not(:disabled){border-color:#d2a921;box-shadow:0 5px 16px rgba(41,37,36,.08);transform:translateY(-1px)}.tile-icon{width:38px;height:38px;border-radius:9px;background:var(--yellow-light);color:var(--red-dark);display:grid;place-items:center;font-size:.72rem;font-weight:800;margin-bottom:10px}.tile-copy{display:block;min-height:43px}.tile-copy strong,.tile-copy small{display:block}.tile-copy strong{font-size:.82rem;line-height:1.25}.tile-copy small{font-size:.68rem;color:var(--muted);margin-top:4px}.product-grid b{font-size:.87rem;color:var(--red-dark);margin-top:auto}.product-grid button:disabled{opacity:.62;cursor:not-allowed}.sold-out{position:absolute;right:8px;top:8px;background:#fff0ee;color:var(--red-dark);font-size:.62rem;font-weight:800;padding:4px 6px;border-radius:7px}@media(max-width:1250px){.product-grid{grid-template-columns:repeat(2,1fr)}}@media(max-width:500px){.product-grid{grid-template-columns:1fr}}`],changeDetection:ChangeDetectionStrategy.OnPush})export class ProductGridComponent{readonly title=input('Produtos frequentes');readonly products=input.required<Product[]>();readonly selected=output<Product>();}
