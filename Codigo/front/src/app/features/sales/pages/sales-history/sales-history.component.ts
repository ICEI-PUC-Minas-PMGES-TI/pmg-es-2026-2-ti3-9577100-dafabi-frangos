import { CurrencyPipe, DatePipe } from '@angular/common';
import { ChangeDetectionStrategy, Component, computed, inject, signal } from '@angular/core';
import { Router, RouterLink } from '@angular/router';
import { MatButtonModule } from '@angular/material/button';
import { MatIconModule } from '@angular/material/icon';
import { EmptyStateComponent } from '../../../../shared/components/empty-state/empty-state.component';
import { PageHeaderComponent } from '../../../../shared/components/page-header/page-header.component';
import { SearchInputComponent } from '../../../../shared/components/search-input/search-input.component';
import { StatusChipComponent } from '../../../../shared/components/status-chip/status-chip.component';
import { SalesService } from '../../services/sales.service';

@Component({selector:'app-sales-history',standalone:true,imports:[CurrencyPipe,DatePipe,RouterLink,MatButtonModule,MatIconModule,EmptyStateComponent,PageHeaderComponent,SearchInputComponent,StatusChipComponent],
  template:`<app-page-header eyebrow="Vendas" title="Histórico de vendas" subtitle="Consulte vendas do balcão, iFood e 99Food em um único lugar." actionLabel="Nova venda" actionIcon="add_shopping_cart" (action)="goNew()"/><div class="toolbar"><app-search-input placeholder="Buscar número ou operador" [value]="query()" (changed)="query.set($event)"/><input class="filter-select" type="date" aria-label="Data inicial" [value]="start()" (change)="start.set($any($event.target).value)"><input class="filter-select" type="date" aria-label="Data final" [value]="end()" (change)="end.set($any($event.target).value)"><select class="filter-select" aria-label="Origem" (change)="origin.set($any($event.target).value)"><option value="ALL">Todas as origens</option><option value="COUNTER">Balcão</option><option value="IFOOD">iFood</option><option value="99FOOD">99Food</option></select><select class="filter-select" aria-label="Pagamento" (change)="payment.set($any($event.target).value)"><option value="ALL">Todos os pagamentos</option><option value="CASH">Dinheiro</option><option value="PIX">Pix</option><option value="DEBIT">Débito</option><option value="CREDIT">Crédito</option></select><select class="filter-select" aria-label="Status" (change)="status.set($any($event.target).value)"><option value="ALL">Todos os status</option><option value="COMPLETED">Concluída</option><option value="CANCELED">Cancelada</option><option value="REFUNDED">Estornada</option></select></div><section class="surface"><div class="surface-header"><div><h2>Vendas encontradas</h2><span class="cell-meta">{{filtered().length}} registro(s)</span></div><strong>Total líquido: {{netTotal()|currency:'BRL':'symbol':'1.2-2':'pt-BR'}}</strong></div>@if(!filtered().length){<app-empty-state icon="receipt_long" title="Nenhuma venda encontrada" message="Não há vendas para os filtros selecionados."/>}@else{<div class="table-wrap"><table class="data-table"><thead><tr><th>Venda</th><th>Data e hora</th><th>Operador</th><th>Origem</th><th>Pagamento</th><th>Bruto</th><th>Descontos</th><th>Taxas</th><th>Líquido</th><th>Status</th><th><span class="sr-only">Ações</span></th></tr></thead><tbody>@for(sale of filtered();track sale.id){<tr><td class="cell-title">{{sale.number}}</td><td>{{sale.createdAt|date:'dd/MM/yyyy HH:mm'}}</td><td>{{sale.operator}}</td><td><span class="origin" [class.ifood]="sale.origin==='IFOOD'" [class.food99]="sale.origin==='99FOOD'">{{originLabel(sale.origin)}}</span></td><td>{{paymentLabel(sale.payment.method)}}</td><td>{{sale.gross|currency:'BRL':'symbol':'1.2-2':'pt-BR'}}</td><td>{{sale.discount|currency:'BRL':'symbol':'1.2-2':'pt-BR'}}</td><td>{{sale.fees|currency:'BRL':'symbol':'1.2-2':'pt-BR'}}</td><td class="money">{{sale.net|currency:'BRL':'symbol':'1.2-2':'pt-BR'}}</td><td><app-status-chip [status]="sale.status"/></td><td><a mat-icon-button [routerLink]="['/app/vendas',sale.id]" [attr.aria-label]="'Ver venda '+sale.number"><mat-icon>chevron_right</mat-icon></a></td></tr>}</tbody></table></div>}</section>`,styles:[`.origin{font-size:.72rem;font-weight:700;padding:5px 8px;border-radius:7px;background:#f3f4f6}.origin.ifood{background:#ffe8e8;color:#9e1818}.origin.food99{background:#fff4cf;color:#5e4900}`],changeDetection:ChangeDetectionStrategy.OnPush})
export class SalesHistoryComponent {
  readonly service=inject(SalesService); private readonly router=inject(Router);
  readonly query=signal('');readonly origin=signal('ALL');readonly payment=signal('ALL');readonly status=signal('ALL');readonly start=signal('2026-09-01');readonly end=signal('2026-09-30');
  readonly filtered=computed(()=>this.service.sales().filter(sale=>(this.origin()==='ALL'||sale.origin===this.origin())&&(this.payment()==='ALL'||sale.payment.method===this.payment())&&(this.status()==='ALL'||sale.status===this.status())&&(!this.start()||sale.createdAt.slice(0,10)>=this.start())&&(!this.end()||sale.createdAt.slice(0,10)<=this.end())&&`${sale.number} ${sale.operator}`.toLowerCase().includes(this.query().toLowerCase())));
  readonly netTotal=computed(()=>this.filtered().reduce((sum,sale)=>sum+sale.net,0));
  goNew(){void this.router.navigate(['/app/vendas/nova']);}
  originLabel(value:string){return{COUNTER:'Balcão',IFOOD:'iFood','99FOOD':'99Food'}[value]??value;}paymentLabel(value:string){return{CASH:'Dinheiro',PIX:'Pix',DEBIT:'Débito',CREDIT:'Crédito'}[value]??value;}
}
