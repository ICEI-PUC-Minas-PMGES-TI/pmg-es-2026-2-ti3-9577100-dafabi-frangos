import { ChangeDetectionStrategy, Component, inject } from '@angular/core';
import { MatButtonModule } from '@angular/material/button'; import { MatIconModule } from '@angular/material/icon'; import { MatSlideToggleModule } from '@angular/material/slide-toggle'; import { MatSnackBar } from '@angular/material/snack-bar';
import { PageHeaderComponent } from '../../../../shared/components/page-header/page-header.component'; import { StatusChipComponent } from '../../../../shared/components/status-chip/status-chip.component'; import { SettingsNavComponent } from '../../components/settings-nav/settings-nav.component'; import { SettingsService } from '../../services/settings.service';

@Component({selector:'app-users',standalone:true,imports:[MatButtonModule,MatIconModule,MatSlideToggleModule,PageHeaderComponent,StatusChipComponent,SettingsNavComponent],template:`<app-page-header eyebrow="Configurações" title="Usuários" subtitle="Controle quem pode acessar o sistema e o perfil de cada pessoa."/><app-settings-nav/><section class="surface"><div class="surface-header"><div><h2>Usuários cadastrados</h2><span class="cell-meta">{{service.users().length}} pessoas</span></div><button mat-flat-button class="primary-button" (click)="addUser()"><mat-icon>person_add</mat-icon>Novo operador</button></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Usuário</th><th>E-mail</th><th>Perfil</th><th>Status</th><th>Acesso</th></tr></thead><tbody>@for(u of service.users();track u.id){<tr><td><div class="user-cell"><span>{{initials(u.name)}}</span><strong>{{u.name}}</strong></div></td><td>{{u.email}}</td><td>{{u.role==='ADMIN'?'Administradora':'Operador de caixa'}}</td><td><app-status-chip [status]="u.active?'ACTIVE':'INACTIVE'"/></td><td><mat-slide-toggle [checked]="u.active" (change)="toggle(u.id)" [attr.aria-label]="'Alterar acesso de '+u.name">{{u.active?'Liberado':'Bloqueado'}}</mat-slide-toggle></td></tr>}</tbody></table></div></section>`,styles:[`.user-cell{display:flex;align-items:center;gap:10px}.user-cell span{width:34px;height:34px;border-radius:50%;background:var(--yellow-light);color:var(--red-dark);display:grid;place-items:center;font-size:.68rem;font-weight:800}`],changeDetection:ChangeDetectionStrategy.OnPush})
export class UsersComponent{readonly service=inject(SettingsService);private readonly snack=inject(MatSnackBar);initials(name:string){return name.split(' ').slice(0,2).map(x=>x[0]).join('')}toggle(id:string){this.service.toggleUser(id);this.snack.open('Acesso do usuário atualizado.','Fechar',{duration:3000})}addUser(){this.service.addCashier();this.snack.open('Novo operador adicionado para demonstração.','Fechar',{duration:3500})}}
